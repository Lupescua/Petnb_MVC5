@model IEnumerable<Petnb_MVC5.Models.SitterUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SitterUserId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SitterUserId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SitterUserId })
        </td>
    </tr>
}

</table>
